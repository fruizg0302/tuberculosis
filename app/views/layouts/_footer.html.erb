<div class="row">
  <div class="text-center"></div>
   <footer class="footer">
    <nav>
      <ul>
        <% if user_signed_in? %>
            <p>Hola <%= current_user.email %>.</p>
        <% end %>
      </ul>
    </nav>
  </footer> 
</div>
